// Package pagination provides the limit/offset primitives the offset-based
// endpoints share. Cursor-paginated surfaces (feed, post lists, notifications)
// carry their own opaque cursors instead, so nothing here computes page numbers.
package pagination
