// Package service implements feed orchestration through focused timeline,
// mixed-source blending, trending, discovery, follow-resolution, and response
// enrichment components. Cursor transitions are kept as pure operations while
// FeedService preserves the feature's public application-service boundary.
package service
