// Package dto defines request and response payloads for user and auth operations.
//
// `binding:"required"` tags only mark fields required in the generated OpenAPI
// schema; nothing binds on them. The service layer's validation enforces them,
// so keep the two in step.
package dto
