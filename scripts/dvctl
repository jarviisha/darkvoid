#!/usr/bin/env bash
# Same release, project and file selection as dv; no implicit infrastructure boot.
set -euo pipefail
script_dir="$(CDPATH='' cd -- "$(dirname "$0")" && pwd)"
exec bash "$script_dir/dv" --profile tools run --rm --no-deps \
	-e DARKVOIDCTL_PASSWORD ctl "$@"
