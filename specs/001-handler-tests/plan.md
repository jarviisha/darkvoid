# Implementation Plan: user/handler Meaningful Test Coverage

**Branch**: `001-handler-tests` | **Date**: 2026-04-27 | **Spec**: user input (no spec.md)
**Input**: Coverage analysis of `internal/feature/user/handler/` at 68.4%

## Summary

Add meaningful test coverage for `internal/feature/user/handler/` targeting real behavioral gaps:
the completely untested `EmailHandler` (4 security-critical flows), missing authorization-gate
tests in `UpdateUser`/`DeactivateUser`, mobile/web client divergence in auth flows, untested file
upload handlers (`UploadAvatar`/`UploadCover`), and the `?by=username` resolution path in
`GetUserProfile`. Coverage is a side-effect, not the goal — every test verifies behavior a
regression could break.

## Technical Context

**Language/Version**: Go 1.26  
**Primary Dependencies**: `net/http/httptest`, `mime/multipart` (stdlib); `github.com/go-chi/chi/v5`, `github.com/google/uuid`  
**Storage**: N/A  
**Testing**: standard `testing` package only; `*_test.go` co-located in `internal/feature/user/handler/`  
**Target Platform**: Linux (CI)  
**Project Type**: test additions only — no production code changes  
**Performance Goals**: N/A  
**Constraints**: `make lint` and `make test` must pass; no third-party test frameworks  
**Scale/Scope**: ~30 new test functions across 5 test files (1 new, 4 extended)

## Constitution Check

| Principle | Status | Notes |
|---|---|---|
| I — Code Quality (`make lint` passes) | ✅ PASS | No production code changes; test code must also pass lint |
| I — `pkg/logger` only | ✅ PASS | Tests don't call logger directly |
| I — No cross-context imports | ✅ PASS | Tests stay within `handler` package |
| II — Tests for every logic change | ✅ PASS | This PR *is* the tests; existing gaps now covered |
| II — `Test<Subject>_<Scenario>` naming | ✅ PASS | All new tests follow this convention |
| II — `make test` passes before commit | ✅ GATE | Must verify locally |
| III — No API contract changes | ✅ PASS | Test-only |
| IV — No performance-path changes | ✅ PASS | Test-only |

**Gate result**: No violations. Proceed.

## Project Structure

### Documentation (this feature)

```text
specs/001-handler-tests/
├── plan.md       ← this file
├── research.md   ← gap analysis and decision log
└── tasks.md      ← generated by /speckit-tasks
```

### Source Code (files touched)

```text
internal/feature/user/handler/
├── email_handler_test.go      ← NEW (currently 0% coverage)
├── auth_handler_test.go       ← EXTEND (~6 new tests)
├── user_handler_test.go       ← EXTEND (~2 new tests)
├── profile_handler_test.go    ← EXTEND (~8 new tests)
└── follow_handler_test.go     ← EXTEND (~1 new test)
```

No production files are modified.

## Implementation Detail

### 1. `email_handler_test.go` — new file

Requires a new `mockEmailService` struct implementing `emailService` interface.

| Test | Scenario verified |
|---|---|
| `TestVerifyEmail_Success` | Token accepted → 200 |
| `TestVerifyEmail_InvalidJSON` | Malformed body → 400 |
| `TestVerifyEmail_ServiceError` | Invalid/expired token → service error propagated |
| `TestResendVerification_Success` | Always 200 (no account-existence leak) |
| `TestResendVerification_InvalidJSON` | 400 |
| `TestResendVerification_ServiceError` | Error propagated (still 200-ish; verify service is called) |
| `TestForgotPassword_Success` | Always 200 (no account-existence leak) |
| `TestForgotPassword_InvalidJSON` | 400 |
| `TestForgotPassword_ServiceError` | Error propagated |
| `TestResetPassword_Success` | Token + new password accepted → 200 |
| `TestResetPassword_InvalidJSON` | 400 |
| `TestResetPassword_ServiceError` | Invalid/expired token error propagated |

Key assertion for `ResendVerification_Success` and `ForgotPassword_Success`: response is 200
regardless of whether the email exists — verify the obscurity behavior is preserved, not just
that the status code is 200.

### 2. `auth_handler_test.go` — additions

| Test | Scenario verified |
|---|---|
| `TestRefreshToken_MobileSuccess` | `X-Client-Type: mobile`, token in body → new token in JSON body, **no** Set-Cookie header |
| `TestRefreshToken_WebMissingCookie` | No cookie, no mobile header → 401 |
| `TestLogout_MobileSuccess` | `X-Client-Type: mobile`, token in body → service called, **no** Set-Cookie header |
| `TestLogout_ServiceError` | Service returns error → error response propagated |
| `TestLogoutAllSessions_ServiceError` | Service returns error → error response propagated |
| `TestLogoutAllSessions_MobileNoCookieClear` | `X-Client-Type: mobile` → 200, **no** Set-Cookie header |

### 3. `user_handler_test.go` — additions

| Test | Scenario verified |
|---|---|
| `TestUpdateUser_Forbidden` | Authenticated user A tries to update user B → 403 FORBIDDEN |
| `TestDeactivateUser_Forbidden` | Authenticated user A tries to deactivate user B → 403 FORBIDDEN |

Both tests exercise the `currentUserID == nil \|\| resolved.ID != *currentUserID` guard.
`TestDeactivateUser_Forbidden` should also verify `TestDeactivateUser_NotAuthenticated` (nil userID path)
if not already covered — check existing tests before adding.

### 4. `profile_handler_test.go` — additions

Requires a new `mockFollowChecker` struct implementing `followChecker` interface.
`newProfileHandlerWithChecker(svc, checker)` helper may be needed alongside existing
`newProfileHandler`.

| Test | Scenario verified |
|---|---|
| `TestGetUserProfile_ByUsername` | `?by=username` → `resolver.GetUserByUsername` called, `GetProfileByUserID` NOT called (resolved.User reused) |
| `TestGetUserProfile_ByUsernameNotFound` | `?by=username` resolver error → 404 |
| `TestGetUserProfile_IsFollowingEnriched` | Authenticated viewer ≠ target → `followChecker.IsFollowing` called, `is_following` field set in response |
| `TestUploadAvatar_Success` | Multipart body with `file` field → 200 with user response |
| `TestUploadAvatar_NotAuthenticated` | No user in context → 401 |
| `TestUploadAvatar_MissingFile` | Multipart without `file` field → 400 |
| `TestUploadAvatar_ServiceError` | Service returns error → error propagated |
| `TestUploadCover_Success` | Multipart body with `file` field → 200 |
| `TestUploadCover_NotAuthenticated` | 401 |
| `TestUploadCover_MissingFile` | 400 |
| `TestUploadCover_ServiceError` | Error propagated |

Multipart helper pattern:
```go
func multipartRequest(t *testing.T, url, field, filename, content string) *http.Request {
    t.Helper()
    var buf bytes.Buffer
    w := multipart.NewWriter(&buf)
    fw, _ := w.CreateFormFile(field, filename)
    _, _ = fw.Write([]byte(content))
    w.Close()
    req, _ := http.NewRequestWithContext(context.Background(), http.MethodPut, url, &buf)
    req.Header.Set("Content-Type", w.FormDataContentType())
    return req
}
```

### 5. `follow_handler_test.go` — addition

| Test | Scenario verified |
|---|---|
| `TestGetFollowing_ServiceError` | Service error → error propagated |

## Complexity Tracking

> No constitution violations — section not applicable.
